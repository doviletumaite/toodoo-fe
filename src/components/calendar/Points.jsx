const Points = (day) => {
    
    function isSelected(day, value){
        return value.isSame(day, "day")
        console.log(day)
    }
    return (
        <div>
        </div>
    )
}
export default Points